// Weekly plan page — stub, wire up in next session
export default function WeeklyPlan() {
  return (
    <div style={{ padding:20, fontSize:13, color:'var(--text-2)' }}>
      Weekly plan — coming next session
    </div>
  )
}
