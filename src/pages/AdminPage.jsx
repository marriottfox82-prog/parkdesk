// Admin page — stub, wire up in next session
export default function AdminPage() {
  return (
    <div style={{ padding:20, fontSize:13, color:'var(--text-2)' }}>
      Admin — coming next session
    </div>
  )
}
